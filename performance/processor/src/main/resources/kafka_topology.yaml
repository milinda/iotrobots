zk.servers: ["10.39.1.21:2181"]
zk.root: "/iot/sensors"
topology.name: "wordcount"
spouts:
    sentence_receive:
        broker: "kafka"
        sensor: "data_sensor"
        channel: "data_sender"
        fields: ["body", "sensorID", "time"]
        properties:
          ackMode: "auto"
          broker.zk.servers: "10.39.1.21:2181"
          broker.zk.root: "/brokers"
bolts:
    count_send:
        broker: "kafka"
        sensor: "data_sensor"
        channel: "data_sender"
        fields: ["body", "sensorID", "time"]
        properties:
          request.required.acks: "0"
          metadata.broker.list: "10.39.1.21:9092"