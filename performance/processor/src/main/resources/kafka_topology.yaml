zk.servers: ["10.39.1.21:2181"]
zk.root: "/iot/sensors"
topology.name: "wordcount"
spouts:
    data_receive:
        broker: "kafka"
        sensor: "data_sensor"
        channel: "data_sender"
        fields: ["body", "sensorID", "time"]
        properties:
          ackMode: "auto"
          broker.zk.servers: "10.39.1.21:2181"
          broker.zk.root: "/brokers"
bolts:
    data_send:
        broker: "kafka"
        sensor: "data_sensor"
        channel: "data_receiver"
        fields: ["body", "sensorID", "time"]
        properties:
          request.required.acks: "0"
          metadata.broker.list: "10.39.1.45:9092,10.39.1.46:9092"